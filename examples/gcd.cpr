fn I32 main() {
    I32 a, b;
    if (!read(&a) || !read(&b) || a < 0 || b < 0) {
        return 1;
    }

    while (a != 0 && b != 0) {
        if (a > b) {
            a = a % b;
        } else {
            b = b % a;
        }
    }

    println("gcd=", a + b);

    return 0;
}
